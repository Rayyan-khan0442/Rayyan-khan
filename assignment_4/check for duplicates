#include <iostream>
#include <vector>
using namespace std;

int main() {
    int N;
    cout << "Enter number of elements: ";
    cin >> N;

    vector<int> nums(N);
    cout << "Enter elements:\n";
    for (int i = 0; i < N; i++)
        cin >> nums[i];

    bool hasDuplicate = false;
    for (int i = 0; i < N; i++) {
        for (int j = i+1; j < N; j++) {
            if (nums[i] == nums[j]) {
                hasDuplicate = true;
                break;
            }
        }
        if (hasDuplicate) break;
    }

    if (hasDuplicate)
        cout << "Duplicates found.\n";
    else
        cout << "No duplicates.\n";

    return 0;
}
